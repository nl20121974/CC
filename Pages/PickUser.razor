@page "/pickuser"
@inherits Models.PickUserModel

<h3>PickUser</h3>
@if (Members != null)
{
    <ul>
        @foreach (var member in Members)
        {
            <li>

                <button class="btn btn-primary" @onclick="()=>PickMember(member)">@member.Name</button>
            </li>
        }
    </ul>
}