@page
@model CC.Pages.EditModel

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Edit</h1>

<h4>UserGroup</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="UserGroup.Id" />
            <div class="form-group">
                <label asp-for="UserGroup.GroupId" class="control-label"></label>
                <select asp-for="UserGroup.GroupId" class="form-control" asp-items="ViewBag.GroupId"></select>
                <span asp-validation-for="UserGroup.GroupId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="UserGroup.UserId" class="control-label"></label>
                <select asp-for="UserGroup.UserId" class="form-control" asp-items="ViewBag.UserId"></select>
                <span asp-validation-for="UserGroup.UserId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
