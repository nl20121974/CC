@page
@model CC.Pages.DetailsModel

@{
    ViewData["Title"] = "Details";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Details</h1>

<div>
    <h4>UserGroup</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.UserGroup.Group)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.UserGroup.Group.Code)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.UserGroup.User)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.UserGroup.User.Id)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.UserGroup?.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
